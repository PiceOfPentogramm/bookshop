# Copy this file to .env and fill in your own values.
# The .env file is listed in .gitignore and will never be committed.

# PostgreSQL credentials
POSTGRES_USER=bookshop_user
POSTGRES_PASSWORD=your_strong_postgres_password

# RabbitMQ credentials
RABBITMQ_DEFAULT_USER=bookshop_rabbit
RABBITMQ_DEFAULT_PASS=your_strong_rabbitmq_password

# JWT / application secret key (use a long random string)
SECRET_KEY=your_very_long_and_random_secret_key_here
